<!DOCTYPE html>
<html>
<head>
  <title>Refactor App</title>
  <link rel="stylesheet" href="../static/styles.css">
</head>
<body>

  <h1>Event</h1>
  <form id="eventForm" class="form" action="/admin/dashboard" method="POST">
    
    <button>Add</button>
     <label><h3>Event Title</h3></label>
    <input id="event" name="event" placeholder="Event title">

    <h3>Event Description</h3>
    <input id="description" name="description" placeholder="Breif overview of event">
    <% buildings.forEach(b => { %>
      <div class="formOption">
          <h3><%= b.building %></h3>
          <% b.rooms.forEach((r, i) => {%>
            <li class="roomCheck" >
              <ul><input type="checkbox" name="room[Science]" value=<%=r%> id=<%=b.building+i%> class="room">
              <label for=<%=b.building+i%>><%=r%></label> </ul>
              <ul><input placeholder="Description" style="display:block" ></ul>
            </li>
          <% }) %>        
      </div>
    <% }) %>
  </form>

  
</body>
</html> 
<!-- <script>function toggleVisibility(i) {
    if(i.checked == true){
      alert("hiii");
      i.style = "display:block";}
    else {i.style="display:none";}
  } 
</script> -->
